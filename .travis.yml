
language:
- node_js:
before_install:
  - npm install -g lerna
  - npm install -g nyc
install:
  - cd packages/etherlime-utils
  - npm link
  - cd ..
  - cd etherlime
  - npm link etherlime-utils@1.0.0
  - npm link
  - cd ../..
  - lerna bootstrap
  - lerna link
  - npm link
services:
  - docker
script:
  - etherlime ganache &> /dev/null &
  - sleep 10
  - lerna run test
after_success: 
  - nyc report --reporter=text-lcov > coverage.lcov && codecov