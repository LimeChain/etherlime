language:
- node_js:
before_install:
  - npm install -g lerna
install:
  - ./bin/travis-bootstrap.sh
services:
  - docker
script:
  - etherlime ganache &> /dev/null &
  - sleep 10
  - ./bin/travis-test.sh
after_success: 
  - lerna run coverage
